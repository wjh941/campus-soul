import { ArrowRight, Ban, LockKeyhole, ShieldCheck, Sparkles, User } from 'lucide-react'
import styles from './GuestActivation.module.css'

export default function GuestActivation({onExplore,onAuth}:{onExplore:()=>void;onAuth:()=>void}){return <section className={`${styles['guest-activation']} glass-card`} aria-label="首次使用引导"><div className="guest-activation-copy"><span className="section-kicker"><Sparkles size={14}/> FIRST 3 MINUTES</span><h2>先用 3 道题，看看你会遇见怎样的同频。</h2><p>无需先填写完整资料。完成轻量自测后，再决定是否注册和开启附近发现。</p><div className={styles['guest-trust']}><span><ShieldCheck size={15}/>仅面向 18+ 用户</span><span><LockKeyhole size={15}/>资料和位置可控</span><span><Ban size={15}/>可随时屏蔽举报</span><span><User size={15}/>不公开邮箱</span></div></div><div className={styles['guest-activation-actions']}><button className="primary" type="button" onClick={onExplore}>开始 3 分钟自测 <ArrowRight size={16}/></button><button className="secondary" type="button" onClick={onAuth}>已有账号，直接登录</button></div></section>}
