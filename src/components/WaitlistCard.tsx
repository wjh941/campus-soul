import{useState}from'react'
import{Check,LoaderCircle,Mail,Sparkles}from'lucide-react'
import{joinWaitlist}from'../lib/commerce'
export default function WaitlistCard(){const[email,setEmail]=useState(''),[city,setCity]=useState(''),[school,setSchool]=useState(''),[referral,setReferral]=useState(''),[consent,setConsent]=useState(false),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState('');const submit=async()=>{if(!email||!consent||busy)return;setBusy(true);setError('');try{await joinWaitlist(email,city,school,referral,consent);setDone(true)}catch(e){setError(e instanceof Error?e.message:'候补登记失败')}finally{setBusy(false)}};return <section className="waitlist-card"><div><span><Sparkles/>INVITATION ACCESS</span><h2>加入同频候补名单</h2><p>我们会按城市和真实用户供需分批开放，避免注册后看不到合适的人。</p></div>{done?<div className="waitlist-done"><Check/><b>登记成功</b><p>获得开放名额后，我们会通过邮箱联系你。</p></div>:<div className="waitlist-form"><label>联系邮箱<input type="email" maxLength={160} value={email} onChange={e=>setEmail(e.target.value.trim())} placeholder="name@example.com"/></label><div><label>所在城市<input maxLength={60} value={city} onChange={e=>setCity(e.target.value)} placeholder="可选"/></label><label>学校 / 单位<input maxLength={100} value={school} onChange={e=>setSchool(e.target.value)} placeholder="可选"/></label></div><label>邀请码<input maxLength={40} value={referral} onChange={e=>setReferral(e.target.value)} placeholder="已有邀请码可填写"/></label><label className="waitlist-consent"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/>同意我们仅使用邮箱通知开放资格，可随时申请删除。</label>{error&&<p className="composer-error">{error}</p>}<button className="primary" disabled={busy||!email||!consent} onClick={()=>void submit()}>{busy?<LoaderCircle className="spin"/>:<Mail/>}登记候补</button></div>}</section>}
